//
// Created by abodi on 16/06/2020.
//
#include "doctest.h"
#include "range.hpp"
#include "accumulate.hpp"
#include "compress.hpp"
#include "filterfalse.hpp"
#include <vector>
#include <string>
using namespace std;
using namespace itertools;
TEST_CASE("testing accumulate for range semi-container"){
    size_t current = 1;
    for(int i : accumulate(range(255,690))){
                CHECK(i == current*(current+1)/2);
        ++current;
    }
}

TEST_CASE("testing range"){
    size_t current = 0;
    for(int itr : range(100,137)){
                CHECK(itr == current++);
    }
}
TEST_CASE("testing accumulate for range semi-container"){
    size_t current = 1;
    for(int i : accumulate(range(255,690))){
                CHECK(i == current*(current+1)/2);
        ++current;
    }
}

TEST_CASE("testing range"){
    size_t current = 0;
    for(int itr : range(100,137)){
                CHECK(itr == current++);
    }
}
TEST_CASE("testing accumulate for range semi-container"){
    size_t current = 1;
    for(int i : accumulate(range(255,690))){
                CHECK(i == current*(current+1)/2);
        ++current;
    }
}

TEST_CASE("testing range"){
    size_t current = 0;
    for(int itr : range(100,137)){
                CHECK(itr == current++);
    }
}
TEST_CASE("testing accumulate for range semi-container"){
    size_t current = 1;
    for(int i : accumulate(range(255,690))){
                CHECK(i == current*(current+1)/2);
        ++current;
    }
}

TEST_CASE("testing range"){
    size_t current = 0;
    for(int itr : range(100,137)){
                CHECK(itr == current++);
    }
}
TEST_CASE("testing accumulate for range semi-container"){
    size_t current = 1;
    for(int i : accumulate(range(255,690))){
                CHECK(i == current*(current+1)/2);
        ++current;
    }
}

TEST_CASE("testing range"){
    size_t current = 0;
    for(int itr : range(100,137)){
                CHECK(itr == current++);
    }
}
TEST_CASE("testing accumulate for range semi-container"){
    size_t current = 1;
    for(int i : accumulate(range(255,690))){
                CHECK(i == current*(current+1)/2);
        ++current;
    }
}

TEST_CASE("testing range"){
    size_t current = 0;
    for(int itr : range(100,137)){
                CHECK(itr == current++);
    }
}
TEST_CASE("testing accumulate for range semi-container"){
    size_t current = 1;
    for(int i : accumulate(range(255,690))){
                CHECK(i == current*(current+1)/2);
        ++current;
    }
}

TEST_CASE("testing range"){
    size_t current = 0;
    for(int itr : range(100,137)){
                CHECK(itr == current++);
    }
}
TEST_CASE("testing accumulate for range semi-container"){
    size_t current = 1;
    for(int i : accumulate(range(255,690))){
                CHECK(i == current*(current+1)/2);
        ++current;
    }
}

TEST_CASE("testing range"){
    size_t current = 0;
    for(int itr : range(100,137)){
                CHECK(itr == current++);
    }
}
TEST_CASE("testing accumulate for range semi-container"){
    size_t current = 1;
    for(int i : accumulate(range(255,690))){
                CHECK(i == current*(current+1)/2);
        ++current;
    }
}

TEST_CASE("testing range"){
    size_t current = 0;
    for(int itr : range(100,137)){
                CHECK(itr == current++);
    }
}
TEST_CASE("testing accumulate for range semi-container"){
    size_t current = 1;
    for(int i : accumulate(range(255,690))){
                CHECK(i == current*(current+1)/2);
        ++current;
    }
}

TEST_CASE("testing range"){
    size_t current = 0;
    for(int itr : range(100,137)){
                CHECK(itr == current++);
    }
}
TEST_CASE("testing accumulate for range semi-container"){
    size_t current = 1;
    for(int i : accumulate(range(255,690))){
                CHECK(i == current*(current+1)/2);
        ++current;
    }
}

TEST_CASE("testing range"){
    size_t current = 0;
    for(int itr : range(100,137)){
                CHECK(itr == current++);
    }
}
TEST_CASE("testing accumulate for range semi-container"){
    size_t current = 1;
    for(int i : accumulate(range(255,690))){
                CHECK(i == current*(current+1)/2);
        ++current;
    }
}

TEST_CASE("testing range"){
    size_t current = 0;
    for(int itr : range(100,137)){
                CHECK(itr == current++);
    }
}
TEST_CASE("testing accumulate for range semi-container"){
    size_t current = 1;
    for(int i : accumulate(range(255,690))){
                CHECK(i == current*(current+1)/2);
        ++current;
    }
}

TEST_CASE("testing range"){
    size_t current = 0;
    for(int itr : range(100,137)){
                CHECK(itr == current++);
    }
}
TEST_CASE("testing accumulate for range semi-container"){
    size_t current = 1;
    for(int i : accumulate(range(255,690))){
                CHECK(i == current*(current+1)/2);
        ++current;
    }
}

TEST_CASE("testing range"){
    size_t current = 0;
    for(int itr : range(100,137)){
                CHECK(itr == current++);
    }
}
TEST_CASE("testing accumulate for range semi-container"){
    size_t current = 1;
    for(int i : accumulate(range(255,690))){
                CHECK(i == current*(current+1)/2);
        ++current;
    }
}

TEST_CASE("testing range"){
    size_t current = 0;
    for(int itr : range(100,137)){
                CHECK(itr == current++);
    }
}
TEST_CASE("testing accumulate for range semi-container"){
    size_t current = 1;
    for(int i : accumulate(range(255,690))){
                CHECK(i == current*(current+1)/2);
        ++current;
    }
}

TEST_CASE("testing range"){
    size_t current = 0;
    for(int itr : range(100,137)){
                CHECK(itr == current++);
    }
}
TEST_CASE("testing accumulate for range semi-container"){
    size_t current = 1;
    for(int i : accumulate(range(255,690))){
                CHECK(i == current*(current+1)/2);
        ++current;
    }
}

TEST_CASE("testing range"){
    size_t current = 0;
    for(int itr : range(100,137)){
                CHECK(itr == current++);
    }
}





